# Subjects

Each subject folder contains:

- `INDEX.md` for the workspace list
- `subject.json` for machine-readable subject metadata
- `workspaces/` for leaf workspaces

Subject folders are passive containers. Use the root registrar to create subjects, choose or create kinds, and create workspaces. Then move directly into the generated workspace folder for operational work.
