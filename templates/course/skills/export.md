# Skill: Export

Use this skill when exporting notes to PDF.

## Default Behavior

- Export from the repo root with `scripts/md_to_pdf.py`.
- Keep the PDF name aligned with the markdown filename.
- Put exports in `notes/exports/`.
- If the export is stale, say so clearly.
